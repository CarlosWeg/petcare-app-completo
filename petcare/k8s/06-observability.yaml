apiVersion: v1
kind: ConfigMap
metadata:
  name: observability-info
  namespace: petcare
data:
  observability.md: |
    Observabilidade básica do projeto:
    - Logs dos pods via kubectl logs.
    - Status dos pods via kubectl get pods.
    - Logs da Lambda no AWS CloudWatch.
    - Eventos do Kubernetes via kubectl get events.

    Comandos úteis:
    kubectl get pods -n petcare
    kubectl logs -n petcare deployment/backend
    kubectl logs -n petcare deployment/worker
    kubectl get events -n petcare --sort-by=.metadata.creationTimestamp

#Script exemplo caso seja implementado algo para utilizar sistema de observabilidade no backend

# apiVersion: apps/v1
# kind: Deployment
# metadata:
#   name: grafana
#   namespace: petcare
#   labels:
#     app: grafana
# spec:
#   replicas: 1
#   selector:
#     matchLabels:
#       app: grafana
#   template:
#     metadata:
#       labels:
#         app: grafana
#     spec:
#       containers:
#         - name: grafana
#           image: grafana/grafana:latest
#           imagePullPolicy: IfNotPresent
#           ports:
#             - containerPort: 3000
#           env:
#             - name: GF_SECURITY_ADMIN_USER
#               value: "admin"
#             - name: GF_SECURITY_ADMIN_PASSWORD
#               value: "admin"
#           resources:
#             requests:
#               cpu: "100m"
#               memory: "128Mi"
#             limits:
#               cpu: "500m"
#               memory: "512Mi"

# ---
# apiVersion: v1
# kind: Service
# metadata:
#   name: grafana-service
#   namespace: petcare
# spec:
#   type: NodePort
#   selector:
#     app: grafana
#   ports:
#     - protocol: TCP
#       port: 3000
#       targetPort: 3000
#       nodePort: 30300